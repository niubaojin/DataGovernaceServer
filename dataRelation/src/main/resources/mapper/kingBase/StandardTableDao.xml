<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
    "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.synway.datarelation.dao.databloodline.StandardTableDao">

    <select id="getTableNameById" resultType="java.lang.String">
         select TABLENAME from synlte.OBJECT where deleted = 0 and relate_tablename = 'OBJECTFIELD'
         and upper(tableid) = upper(#{tableId}) and rownum =1
    </select>

</mapper>
